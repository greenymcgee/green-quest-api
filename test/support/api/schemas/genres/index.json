{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://greenquestgames.com/genres/index.json",
  "description": "The GET /api/genres payload",
  "properties": {
    "genres": {
      "properties": {
        "createdAt": {
          "description": "The datetime of the record's creation",
          "type": "string"
        },
        "id": {
          "description": "The unique identifier for a genre.",
          "type": "integer"
        },
        "igdbId": {
          "description": "The id for the genre in the IGDB database.",
          "type": "integer"
        },
        "name": {
          "description": "The name of the genre",
          "type": "string"
        },
        "slug": {
          "description": "The slug of the genre",
          "type": "string"
        },
        "updatedAt": {
          "description": "The datetime of the record's last update",
          "type": "string"
        }
      },
      "required": ["createdAt", "id", "igdbId", "name", "slug", "updatedAt"]
    }
  },
  "required": ["genres"],
  "type": "object"
}
